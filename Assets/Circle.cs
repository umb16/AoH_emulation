using UnityEngine;
using System.Collections;

public class Circle : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//transform.position = new Vector3(Mathf.Sin(Mathf.Max(3.14f*2,Input.mousePosition.x/100)),Mathf.Sin(Mathf.Max(3.14f*2,Input.mousePosition.y/100)),15);
	}
}
